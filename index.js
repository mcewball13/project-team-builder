let employeeArr = [
    {
    name: "name",
    id: "id",
    phone: "phone",
    getRole()

}
]

const managerArr = employeeArr.filter(employee => employee.getRole() === "Manager");
const managerArr = employeeArr.filter(employee => employee.getRole() === "Manager");
const managerArr = employeeArr.filter(employee => employee.getRole() === "Manager");


managerArr.map(manager => {
    return `<div class="card">${manager.getName()}`
})
managerArr.map(manager => {
    return `<div class="card">${manager.getName()}`
})
managerArr.map(manager => {
    return `<div class="card">${manager.getName()}`
})
